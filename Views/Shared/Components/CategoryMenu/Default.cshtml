@model IEnumerable<CetStudentBook.Models.Category>

@foreach (var category in Model)
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Home" asp-action="Category" asp-route-id="@category.Id">
            @category.Name
        </a>
    </li>
}